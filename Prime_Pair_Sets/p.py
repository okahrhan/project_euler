def is_prime(number):
    for i in range(2,int(number**0.5) + 1):
        if number % i == 0:
            return False
    return True

def list_of_primes():
    lst = []
    count = 2
    for i in range(2,10**4):
        if is_prime

def is_contact(a,b):
    return is_prime(int(str(a))+ int(str(b))) and is_prime(int(str(b))+ int(str(a)))

def lst_c(group,ca)
